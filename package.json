{
  "dependencies": {
    "eth-helpers": "^1.3.1",
    "eth-serde": "^0.0.12",
    "nanoeth": "^2.6.1",
    "tape": "^5.2.2"
  },
  "scripts": {
    "compile": "solc-compile --config=yul.json --source-dir=. --output-dir=./build",
    "lint": "standard ."
  },
  "devDependencies": {
    "standard": "^16.0.3"
  }
}
